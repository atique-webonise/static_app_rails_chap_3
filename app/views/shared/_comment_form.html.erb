<%= form_for([micropost, @comment], :remote => true)  do |f| %>
    <%= render 'shared/error_messages', object: f.object %>
    <%= f.text_area :comment_content, :size => "30x2" %>
    <button class="btn" type="submit">Comment</button>
<% end %>
